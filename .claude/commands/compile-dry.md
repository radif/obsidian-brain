---
description: Preview what compile would do without writing
allowed-tools: Bash(uv run python scripts/compile.py --dry-run:*)
---

Run `uv run python scripts/compile.py --dry-run` and summarize which raw files would be (re)compiled.
