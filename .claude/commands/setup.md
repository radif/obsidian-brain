---
description: One-time bootstrap — install just, uv, and Python deps
allowed-tools: Bash(./scripts/setup.sh:*)
---

Run `./scripts/setup.sh` and report the result. This is idempotent; safe to re-run.
