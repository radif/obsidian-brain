---
description: Preview asset moves without touching files
allowed-tools: Bash(uv run python scripts/collect-assets.py --dry-run:*)
---

Run `uv run python scripts/collect-assets.py --dry-run` and list which files *would* be moved.
